# Legacy Code

This directory preserves notebook-exported code for traceability. The main supported experiment runner is now:

```bash
python scripts/run_polytab.py
```

Use files in this directory only when comparing against the original notebook workflow.
